spring.application.name=Anime Api

# Configuración de MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/anime_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=MeliMeli2024!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA/Hibernate
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Opcional: Para ver consultas con parámetros en logs
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

server.port=8080
